# Tasks — Spec 1: config.py + schemas.py

- [x] Crear `config.py` con carga de variables de entorno usando `python-dotenv` o `os.environ`; incluir GEMINI_API_KEY, OPENAI_API_KEY, WHATSAPP_TOKEN, WHATSAPP_PHONE_ID, WHATSAPP_WEBHOOK_SECRET, MANYCHAT_API_TOKEN, DATABASE_URL, ICN_NEUROASESOR_WHATSAPP, ICN_COORDINADOR_WHATSAPP, TZ
- [x] Definir en `config.py` constantes del sistema: MAX_TOKENS=800, MAX_HISTORIAL_TURNOS=20, SCORE_BAJO_MAX=4, SCORE_MODERADO_MAX=8, SCORE_ALTO_MAX=12, SCORE_ALARMA_MIN=13, D1_FLAG_THRESHOLD=2, SLA_MINUTOS=15, SLA_ALARMA_MINUTOS=5
- [x] Definir en `config.py` la timezone Colombia: `COLOMBIA_TZ = pytz.timezone("America/Bogota")`
- [x] Crear modelo Pydantic `Paciente` en `schemas.py` con todos los campos de la tabla PACIENTES V6 (paciente_id, telefono, nombre, edad, ciudad, aseguramiento, entidad_prepagada, consentimiento_datos_clinicos, consentimiento_teleorientacion, consentimiento_investigacion, timestamp_consentimiento, fase_actual, tipo_consulta, programa_clinico, problema_seleccionado, guia_soporte, cie10, nivel_riesgo, score_total, fenotipo_probable, respuestas_orientacion, reporte_enviado, conversion, fecha_cita, canal_agenda, estado_cita, fecha_primera_orientacion, fecha_ultima_orientacion, cohorte, requiere_humano, requiere_revision_medica, motivo_escalamiento, error_json, error_operativo, version_prompt, version_flujo, historial_json, notas_internas, fecha_creacion, ultima_actividad, prioridad_operativa)
- [x] Crear modelo Pydantic `Conversacion` en `schemas.py` con campos: conv_id, paciente_id, telefono, fase, mensaje_usuario, respuesta_agente, guia_soporte_turno, timestamp, tokens_usados, flag_alarma, escalado_humano, version_prompt
- [x] Crear modelo Pydantic `Cita` en `schemas.py` con campos: cita_id, paciente_id, tipo, programa, fecha_cita, hora_cita, canal_agenda, confirmacion, seguimiento_post, resultado_post
- [x] Crear modelo Pydantic `Escalamiento` en `schemas.py` con campos: escalamiento_id, paciente_id, timestamp_escalamiento, timestamp_respuesta_humano, tiempo_respuesta_humano, cumple_sla_15min, tipo_escalamiento, prioridad_paciente, fase_origen, motivo, resolucion, responsable, resultado
- [x] Crear modelo Pydantic `NeuroAsistenteInteraccion` en `schemas.py` con todos los campos de hoja NEUROASISTENTE_INTERACCIONES (interaccion_id, paciente_id, cita_id, fecha_hora, canal_contacto, tipo_interaccion, mensaje_enviado, respuesta_paciente, estado_interaccion, requiere_revision_clinica, requiere_revision_comercial, notas_internas, responsable, accion_generada, prioridad_paciente, version_flujo, timestamp_actualizacion)
- [x] Crear modelo Pydantic `NeuroAsesorInteraccion` en `schemas.py` con campos: neuroasesor_interaccion_id, paciente_id, fecha_hora_inicio, fecha_hora_fin, tiempo_respuesta_humano, cumple_sla_15min, canal, tipo_interaccion, prioridad_paciente, resumen_conversacion, resultado, oportunidad_detectada, incidencia, notas_internas, responsable, version_flujo
- [x] Crear modelo Pydantic `ResumenDiario` en `schemas.py` con campos: resumen_id, fecha, total_interacciones_na, total_respondidas, total_sin_respuesta, casos_revision_clinica, casos_revision_comercial, total_escalamientos, sla_cumplido_pct, tiempo_respuesta_promedio, total_interacciones_neuroasesor, resumen_narrativo, incidencias_sla, oportunidades_detectadas
- [x] Crear modelo Pydantic `ResponseIA` en `schemas.py` con campos: respuesta_visible (str), notas_internas (str), puntaje_dominio (dict con claves D1-D6, valores int), guia_soporte (str opcional), cie10 (str opcional)
- [x] Crear `requirements.txt` base con: pydantic, python-dotenv, pytz, sqlalchemy, asyncpg, alembic, httpx
